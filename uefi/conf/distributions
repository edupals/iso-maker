Codename: ${SUITE}
Suite: ${SUITE}
Version: ${DISTRIBUTION_VERSION}
Origin: ${DISTRIBUTION_NAME}
Label: ${DISTRIBUTION_NAME}
Description: ${DISTRIBUTION_NAME} distribution packages
Architectures: amd64
Components: main 
Update: - ${SUITE} ${SUITE}-updates ${SUITE}-security
